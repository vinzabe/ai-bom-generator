"""AI-BOM Generator: SBOM-equivalent for ML systems.

Tracks model weights, training/eval data lineage, fine-tuning deltas,
config, license, dependencies; signs the bundle (sigstore-compatible)."""

__version__ = "1.0.0"
